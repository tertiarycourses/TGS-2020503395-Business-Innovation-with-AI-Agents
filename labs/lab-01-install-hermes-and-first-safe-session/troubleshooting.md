# Troubleshooting

If the command is missing, reload the correct shell. If provider authentication fails, re-run `hermes setup` and verify the provider name without exposing the key. Stop on an unrecognised installer or destination.
